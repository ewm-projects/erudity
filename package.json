{
  "name": "erudity",
  "version": "1.0.0",
  "description": "Web application for cataloging, reviewing, sharing, and tracking learning resources",
  "main": "index.js",
  "scripts": {
    "docker:rebuild": "docker compose up -d --build",
    "docker:start": "docker compose up -d",
    "docker:stop": "docker compose down -v",
    "frontend:test": "docker compose exec frontend npm run test",
    "frontend:lint": "docker compose exec frontend npm run lint",
    "frontend:lint:check": "docker compose exec frontend npm run lint:check",
    "frontend:format": "docker compose exec frontend npm run format",
    "frontend:format:check": "docker compose exec frontend npm run format:check",
    "backend:test": "docker compose exec backend npm run test",
    "backend:lint": "docker compose exec backend npm run lint",
    "backend:lint:check": "docker compose exec backend npm run lint:check",
    "backend:format": "docker compose exec backend npm run format",
    "backend:format:check": "docker compose exec backend npm run format:check"
  },
  "author": "",
  "license": "ISC"
}
